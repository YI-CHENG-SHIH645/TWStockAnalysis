import numpy as np
import datetime as dt


def add_two_array(a: np.array, b: np.array, aux: float):
    pass


def show_dic_list(dic_list: dict):
    pass


def trade_on_sids(sids: np.ndarray,
                  o: dict,
                  c: dict,
                  ma20: dict,
                  dates: np.array,
                  selected: dict,
                  holding_days_th: int,
                  dic_records: dict,
                  last_date_signal: dict,
                  sid2tid: dict,
                  today: dt.datetime,
                  available_tid: int,
                  strategy_name: str,
                  trader_code: str):
    pass
